/** @type {import('next').NextConfig} */
const nextConfig = {
  // If needed, add configuration for Cytoscape or other libraries here
  // For example, if you have issues with Cytoscape in Next.js:
  transpilePackages: ['cytoscape'],
}

module.exports = nextConfig
